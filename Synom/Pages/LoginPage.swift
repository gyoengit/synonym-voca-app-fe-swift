import SwiftUI

struct LoginPage: View {
    var body: some View {
        VStack {
            Text("login")
        }
    }
}

#Preview {
    LoginPage()
        .frame(maxWidth: .infinity, maxHeight: .infinity)
        .background(.white)
}
